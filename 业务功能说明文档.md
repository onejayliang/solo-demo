# 宗亲寻根·薪火相传APP - 业务功能说明文档

## 1. 项目概述

### 1.1 项目定位
**宗亲寻根·薪火相传APP** 是一款以"认祖归亲、族谱信息化、宗亲互动"为核心，依托大数据技术，为全球华人宗族、宗亲提供跨地域、智能化、专属化的综合性交流与传承平台。

### 1.2 核心价值
- **文化传承价值**：推动传统族谱信息化、数字化保存
- **宗亲联结价值**：通过大数据技术实现宗亲分支智能匹配
- **社交互动价值**：为认证宗亲提供专属交流空间

### 1.3 技术架构
- **前端**：Vue 3 + Quasar Framework
- **后端**：FastAPI + Python
- **数据库**：PostgreSQL（关系型数据）、MongoDB（文档存储）
- **缓存与实时通信**：Redis、WebSocket
- **搜索与大数据**：Elasticsearch
- **对象存储**：S3兼容对象存储服务

---

## 2. 前端功能说明

### 2.1 认证服务（AuthService）
**文件位置**：[ui/src/services/auth.js](file:///workspace/ui/src/services/auth.js)

#### 功能描述
提供用户注册、登录、登出、获取当前用户信息等功能，确保用户身份安全认证。

#### 主要功能
1. **用户注册** - 允许新用户创建账户，完成基本信息填写
2. **用户登录** - 支持用户名/密码登录，返回JWT令牌
3. **获取当前用户** - 获取当前登录用户的详细信息
4. **用户登出** - 清除本地存储的认证令牌，退出登录状态

#### API调用
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `GET /api/auth/me` - 获取当前用户信息
- `POST /api/auth/logout` - 用户登出

---

### 2.2 祖先数据服务（AncestorDataService）
**文件位置**：[ui/src/services/ancestorData.js](file:///workspace/ui/src/services/ancestorData.js)

#### 功能描述
允许用户提交和管理祖上资料，包括姓氏、堂号、始祖信息、出生信息、职业、成就等内容。

#### 主要功能
1. **提交祖上资料** - 用户可以提交祖先的详细信息
2. **获取我的资料** - 查看用户自己提交的所有祖先资料列表
3. **获取资料详情** - 查看单个祖先资料的详细内容
4. **更新资料** - 修改已提交的祖先资料信息
5. **删除资料** - 删除不再需要的祖先资料

#### API调用
- `POST /api/ancestor-data/submit` - 提交祖上资料
- `GET /api/ancestor-data/my` - 获取我的资料列表
- `GET /api/ancestor-data/{dataId}` - 获取资料详情
- `PUT /api/ancestor-data/{dataId}` - 更新资料
- `DELETE /api/ancestor-data/{dataId}` - 删除资料

---

### 2.3 匹配服务（MatchService）
**文件位置**：[ui/src/services/match.js](file:///workspace/ui/src/services/match.js)

#### 功能描述
基于用户提交的祖先资料，系统自动匹配可能的宗亲分支和潜在宗亲，提供认祖申请功能。

#### 主要功能
1. **获取匹配结果** - 根据资料ID获取系统推荐的匹配宗亲列表
2. **查看匹配详情** - 查看具体的匹配信息，包括相似度分数
3. **提交认祖申请** - 向匹配的宗亲提交认祖申请
4. **获取我的申请** - 查看自己提交的认祖申请列表
5. **审核申请** - 宗族管理员审核其他用户的认祖申请
6. **生成表字** - 审核通过后，系统为用户生成符合宗族规范的表字

#### API调用
- `GET /api/match/relatives/{dataId}` - 获取匹配结果
- `GET /api/match/{matchId}` - 获取匹配详情
- `POST /api/application/submit` - 提交认祖申请
- `GET /api/application/my` - 获取我的申请
- `PUT /api/application/{applicationId}/review` - 审核申请
- `GET /api/style-name/generate` - 生成表字

---

### 2.4 族谱服务（GenealogyService）
**文件位置**：[ui/src/services/genealogy.js](file:///workspace/ui/src/services/genealogy.js)

#### 功能描述
提供族谱的创建、编辑、查阅、下载等功能，支持精细化的权限管理和家族历史文献存储。

#### 主要功能
1. **创建族谱** - 宗族管理员可以创建新的族谱
2. **获取我的族谱** - 查看用户有权限访问的族谱列表
3. **获取族谱详情** - 查看单个族谱的详细信息
4. **更新族谱** - 修改族谱的基本信息
5. **删除族谱** - 删除不再需要的族谱（仅创建者）
6. **添加族员** - 向族谱中添加新的族员信息
7. **获取族员列表** - 查看族谱中的所有族员
8. **上传家族文献** - 上传和管理家族历史文献
9. **设置权限** - 为其他用户设置族谱的查看、编辑、下载权限

#### API调用
- `POST /api/genealogy/create` - 创建族谱
- `GET /api/genealogy/my` - 获取我的族谱
- `GET /api/genealogy/{genealogyId}` - 获取族谱详情
- `PUT /api/genealogy/{genealogyId}` - 更新族谱
- `DELETE /api/genealogy/{genealogyId}` - 删除族谱
- `POST /api/genealogy/{genealogyId}/members` - 添加族员
- `GET /api/genealogy/{genealogyId}/members` - 获取族员列表
- `POST /api/genealogy/{genealogyId}/documents` - 上传文献
- `PUT /api/genealogy/{genealogyId}/permissions` - 设置权限

---

### 2.5 聊天服务（ChatService）
**文件位置**：[ui/src/services/chat.js](file:///workspace/ui/src/services/chat.js)

#### 功能描述
为宗族和宗亲分支提供专属聊天室，支持实时消息通信、活动管理和祖训家风分享。

#### 主要功能
1. **获取聊天室列表** - 查看用户可以加入的所有聊天室
2. **进入聊天室** - 进入指定的聊天室
3. **发送消息** - 在聊天室中发送文本、图片等消息
4. **获取历史消息** - 查看聊天室的历史消息记录
5. **创建活动** - 发布宗族活动信息
6. **获取活动列表** - 查看即将举行的活动
7. **报名活动** - 在线报名参加活动
8. **分享文化** - 发布祖训家风和家族故事
9. **获取文化分享** - 查看其他宗亲分享的文化内容
10. **点赞分享** - 为喜欢的文化分享点赞

#### API调用
- `GET /api/chat/rooms` - 获取聊天室列表
- `GET /api/chat/{chatRoomId}/messages` - 获取历史消息
- `POST /api/chat/{chatRoomId}/messages` - 发送消息
- `WebSocket /api/chat/ws/{chatRoomId}` - 实时通信
- `POST /api/activity/create` - 创建活动
- `GET /api/activity/list` - 获取活动列表
- `POST /api/activity/{activityId}/join` - 报名活动
- `POST /api/culture/share` - 分享文化
- `GET /api/culture/list` - 获取文化分享
- `POST /api/culture/{cultureId}/like` - 点赞分享

---

## 3. 后端功能说明

### 3.1 认证控制器（AuthController）
**文件位置**：[app/app/api/controllers/auth.py](file:///workspace/app/app/api/controllers/auth.py)

#### 功能描述
处理用户认证和身份管理，包括注册、登录、JWT令牌生成和验证。

#### 主要接口
1. **用户注册** - `POST /api/auth/register`
   - 功能：创建新用户账户
   - 输入：用户名、邮箱、密码、姓名等
   - 输出：用户信息和访问令牌
   - 安全：密码使用bcrypt加密存储

2. **用户登录** - `POST /api/auth/login`
   - 功能：验证用户身份并返回JWT令牌
   - 输入：用户名/邮箱、密码
   - 输出：访问令牌、刷新令牌、用户信息

3. **获取当前用户** - `GET /api/auth/me`
   - 功能：获取当前登录用户的详细信息
   - 输入：JWT令牌（请求头）
   - 输出：用户详细信息

4. **刷新令牌** - `POST /api/auth/refresh`
   - 功能：使用刷新令牌获取新的访问令牌
   - 输入：刷新令牌
   - 输出：新的访问令牌

---

### 3.2 祖先数据控制器（AncestorDataController）
**文件位置**：[app/app/api/controllers/ancestor_data.py](file:///workspace/app/app/api/controllers/ancestor_data.py)

#### 功能描述
管理用户提交的祖先数据，包括创建、查询、更新、删除等操作。

#### 主要接口
1. **提交祖先数据** - `POST /api/ancestor-data/submit`
   - 功能：提交新的祖先资料
   - 输入：祖先详细信息（姓氏、名字、出生年份、籍贯、职业等）
   - 输出：创建的祖先数据记录
   - 权限：需要用户登录

2. **获取我的数据** - `GET /api/ancestor-data/my`
   - 功能：获取当前用户提交的所有祖先数据
   - 输入：无
   - 输出：祖先数据列表

3. **获取数据详情** - `GET /api/ancestor-data/{dataId}`
   - 功能：获取单个祖先数据的详细信息
   - 输入：数据ID
   - 输出：祖先数据详情

4. **更新数据** - `PUT /api/ancestor-data/{dataId}`
   - 功能：更新已提交的祖先数据
   - 输入：数据ID和更新内容
   - 输出：更新后的数据
   - 权限：仅数据创建者可更新

5. **删除数据** - `DELETE /api/ancestor-data/{dataId}`
   - 功能：删除祖先数据
   - 输入：数据ID
   - 输出：删除成功消息
   - 权限：仅数据创建者可删除

---

### 3.3 匹配控制器（MatchController）
**文件位置**：[app/app/api/controllers/match.py](file:///workspace/app/app/api/controllers/match.py)

#### 功能描述
基于用户提交的祖先数据，使用大数据匹配算法寻找可能的宗亲分支。

#### 主要接口
1. **获取匹配宗亲** - `GET /api/match/relatives/{dataId}`
   - 功能：根据祖先数据ID获取匹配的宗亲列表
   - 输入：数据ID
   - 输出：匹配的宗亲列表，包含相似度分数
   - 算法：基于多维度特征相似度计算（姓氏、籍贯、字辈、年代等）

2. **获取匹配详情** - `GET /api/match/{matchId}`
   - 功能：获取具体匹配的详细信息
   - 输入：匹配ID
   - 输出：匹配详情，包括双方数据和匹配原因

3. **批量处理匹配** - `POST /api/match/batch`
   - 功能：批量处理未处理的祖先数据匹配
   - 输入：处理数量限制
   - 输出：处理结果统计
   - 权限：管理员权限

4. **生成表字** - `GET /api/style-name/generate`
   - 功能：根据宗族规范生成表字
   - 输入：姓氏、世代、宗族ID
   - 输出：生成的表字

5. **验证表字** - `POST /api/style-name/validate`
   - 功能：验证表字是否符合宗族规范
   - 输入：表字、姓氏、世代、宗族ID
   - 输出：验证结果

---

### 3.4 申请控制器（ApplicationController）
**文件位置**：[app/app/api/controllers/application.py](file:///workspace/app/app/api/controllers/application.py)

#### 功能描述
管理认祖申请的提交、审核流程。

#### 主要接口
1. **提交申请** - `POST /api/application/submit`
   - 功能：提交认祖申请
   - 输入：申请信息（匹配ID、申请理由等）
   - 输出：创建的申请记录
   - 状态：申请状态为"待审核"

2. **获取我的申请** - `GET /api/application/my`
   - 功能：获取当前用户提交的所有申请
   - 输入：无
   - 输出：申请列表

3. **获取待审核申请** - `GET /api/application/pending`
   - 功能：获取待审核的申请列表
   - 输入：无
   - 输出：待审核申请列表
   - 权限：宗族管理员

4. **审核申请** - `PUT /api/application/{applicationId}/review`
   - 功能：审核认祖申请
   - 输入：申请ID、审核状态（通过/拒绝）、审核意见
   - 输出：审核结果
   - 权限：宗族管理员
   - 后续：审核通过后自动生成表字并更新用户宗族身份

---

### 3.5 族谱控制器（GenealogyController）
**文件位置**：[app/app/api/controllers/genealogy.py](file:///workspace/app/app/api/controllers/genealogy.py)

#### 功能描述
提供族谱的创建、编辑、管理和权限控制功能。

#### 主要接口
1. **创建族谱** - `POST /api/genealogy/create`
   - 功能：创建新的族谱
   - 输入：族谱信息（名称、描述、宗族ID等）
   - 输出：创建的族谱记录
   - 权限：需要用户登录

2. **获取我的族谱** - `GET /api/genealogy/my`
   - 功能：获取当前用户有权限访问的族谱列表
   - 输入：无
   - 输出：族谱列表（包括创建的和有权限的）

3. **获取族谱详情** - `GET /api/genealogy/{genealogyId}`
   - 功能：获取族谱的详细信息
   - 输入：族谱ID
   - 输出：族谱详情
   - 权限：需要查看权限

4. **更新族谱** - `PUT /api/genealogy/{genealogyId}`
   - 功能：更新族谱信息
   - 输入：族谱ID和更新内容
   - 输出：更新后的族谱
   - 权限：仅创建者可更新

5. **删除族谱** - `DELETE /api/genealogy/{genealogyId}`
   - 功能：删除族谱
   - 输入：族谱ID
   - 输出：删除成功消息
   - 权限：仅创建者可删除

6. **添加族员** - `POST /api/genealogy/{genealogyId}/members`
   - 功能：向族谱添加族员
   - 输入：族谱ID、族员信息（姓名、性别、出生日期、亲属关系等）
   - 输出：添加的族员记录
   - 权限：需要编辑权限

7. **获取族员列表** - `GET /api/genealogy/{genealogyId}/members`
   - 功能：获取族谱的所有族员
   - 输入：族谱ID
   - 输出：族员列表
   - 权限：需要查看权限

8. **上传文献** - `POST /api/genealogy/{genealogyId}/documents`
   - 功能：上传家族历史文献
   - 输入：族谱ID、文件、名称、描述
   - 输出：上传的文献记录
   - 权限：需要编辑权限
   - 存储：文件存储在对象存储服务中

9. **设置权限** - `PUT /api/genealogy/{genealogyId}/permissions`
   - 功能：为其他用户设置族谱权限
   - 输入：族谱ID、用户ID、权限类型（查看/编辑/下载）
   - 输出：权限设置成功消息
   - 权限：仅创建者可设置权限

---

### 3.6 聊天控制器（ChatController）
**文件位置**：[app/app/api/controllers/chat.py](file:///workspace/app/app/api/controllers/chat.py)

#### 功能描述
提供实时聊天功能，支持群组聊天和消息持久化。

#### 主要接口
1. **获取聊天室列表** - `GET /api/chat/rooms`
   - 功能：获取用户可以加入的聊天室列表
   - 输入：无
   - 输出：聊天室列表

2. **获取历史消息** - `GET /api/chat/{chatRoomId}/messages`
   - 功能：获取聊天室的历史消息
   - 输入：聊天室ID、消息数量限制、时间范围
   - 输出：历史消息列表

3. **发送消息** - `POST /api/chat/{chatRoomId}/messages`
   - 功能：发送消息到聊天室
   - 输入：聊天室ID、消息内容、消息类型
   - 输出：发送的消息记录
   - 实时：同时通过WebSocket推送给在线用户

4. **WebSocket连接** - `WebSocket /api/chat/ws/{chatRoomId}`
   - 功能：建立实时WebSocket连接
   - 输入：聊天室ID
   - 功能：接收实时消息推送
   - 连接管理：自动管理活跃连接

---

### 3.7 活动控制器（ActivityController）
**文件位置**：[app/app/api/controllers/activity.py](file:///workspace/app/app/api/controllers/activity.py)

#### 功能描述
管理宗族活动的创建、发布和报名。

#### 主要接口
1. **创建活动** - `POST /api/activity/create`
   - 功能：创建新的宗族活动
   - 输入：活动信息（名称、描述、时间、地点、人数限制等）
   - 输出：创建的活动记录

2. **获取活动列表** - `GET /api/activity/list`
   - 功能：获取活动列表
   - 输入：无（可添加时间范围筛选）
   - 输出：活动列表（按时间排序）

3. **获取活动详情** - `GET /api/activity/{activityId}`
   - 功能：获取活动的详细信息
   - 输入：活动ID
   - 输出：活动详情

4. **报名活动** - `POST /api/activity/{activityId}/join`
   - 功能：报名参加活动
   - 输入：活动ID
   - 输出：报名成功消息
   - 检查：人数限制、重复报名

5. **获取参与者列表** - `GET /api/activity/{activityId}/participants`
   - 功能：获取活动的参与者列表
   - 输入：活动ID
   - 输出：参与者列表

---

### 3.8 文化分享控制器（CultureController）
**文件位置**：[app/app/api/controllers/culture.py](file:///workspace/app/app/api/controllers/culture.py)

#### 功能描述
提供祖训家风分享和传播功能。

#### 主要接口
1. **分享文化** - `POST /api/culture/share`
   - 功能：发布文化分享（祖训、家风、家族故事等）
   - 输入：分享信息（标题、内容、宗族ID等）
   - 输出：创建的分享记录

2. **获取分享列表** - `GET /api/culture/list`
   - 功能：获取文化分享列表
   - 输入：无（可添加宗族筛选）
   - 输出：分享列表（按点赞数和时间排序）

3. **获取分享详情** - `GET /api/culture/{cultureId}`
   - 功能：获取文化分享的详细信息
   - 输入：分享ID
   - 输出：分享详情

4. **点赞分享** - `POST /api/culture/{cultureId}/like`
   - 功能：为文化分享点赞
   - 输入：分享ID
   - 输出：点赞成功消息和当前点赞数

---

## 4. 大数据处理功能

### 4.1 匹配引擎（MatchEngine）
**文件位置**：[app/app/bigdata/matching.py](file:///workspace/app/app/bigdata/matching.py)

#### 功能描述
实现宗亲匹配算法，基于多维度特征计算相似度。

#### 主要功能
1. **计算相似度** - 基于姓氏、籍贯、字辈、年代、职业、成就等多维度计算相似度
   - 姓氏匹配：权重 0.3
   - 籍贯匹配：权重 0.2
   - 字辈匹配：权重 0.2
   - 年代匹配：权重 0.1-0.2
   - 职业匹配：权重 0.05
   - 成就匹配：权重 0.05

2. **寻找匹配** - 在数据库中查找相似度超过阈值的宗亲
   - 阈值：默认 0.5
   - 去重：避免重复匹配

3. **批量处理** - 批量处理未匹配的祖先数据
   - 处理数量：可配置
   - 自动标记：处理后标记为已处理

---

### 4.2 字辈生成器（StyleNameGenerator）
**文件位置**：[app/app/bigdata/matching.py](file:///workspace/app/app/bigdata/matching.py)

#### 功能描述
根据宗族规范生成和验证表字。

#### 主要功能
1. **生成表字** - 根据姓氏、世代、宗族ID生成符合规范的表字
   - 使用常用字辈库：仁、义、礼、智、信、忠、孝等
   - 世代计算：根据世代数循环使用字辈

2. **验证表字** - 验证表字是否符合宗族规范
   - 检查姓氏匹配
   - 检查字辈字符

---

### 4.3 数据分析器（DataAnalyzer）
**文件位置**：[app/app/bigdata/analytics.py](file:///workspace/app/app/bigdata/analytics.py)

#### 功能描述
提供数据分析和统计功能。

#### 主要功能
1. **祖先数据分布分析** - 分析祖先数据的姓氏、籍贯、年代、字辈分布
2. **匹配统计分析** - 分析匹配数量、平均相似度、相似度分布
3. **族谱统计分析** - 分析族谱数量、成员数量、状态分布
4. **活动统计分析** - 分析活动数量、参与人数、类型分布
5. **文化分享统计** - 分析分享数量、点赞数、宗族分布

---

### 4.4 推荐引擎（RecommendationEngine）
**文件位置**：[app/app/bigdata/recommendation.py](file:///workspace/app/app/bigdata/recommendation.py)

#### 功能描述
基于用户行为和兴趣提供个性化推荐。

#### 主要功能
1. **推荐祖先数据** - 推荐可能相关的祖先数据
   - 基于用户已提交数据的特征
   - 姓氏和籍贯匹配
2. **推荐族谱** - 推荐可能感兴趣的族谱
   - 基于用户参与的族谱
   - 姓氏匹配
3. **推荐活动** - 推荐即将举行的活动
   - 基于用户参与的活动
   - 宗族匹配和时间 proximity
4. **推荐文化分享** - 推荐热门的文化分享
   - 基于宗族和点赞数
5. **个性化推荐** - 整合所有推荐，提供完整的个性化推荐

---

### 4.5 大数据存储（BigDataStorage）
**文件位置**：[app/app/bigdata/storage.py](file:///workspace/app/app/bigdata/storage.py)

#### 功能描述
提供MongoDB和Elasticsearch的集成，支持大数据存储和搜索。

#### 主要功能
1. **MongoDB存储**
   - 存储祖先数据
   - 存储族谱数据
   - 提供聚合查询
2. **Elasticsearch索引**
   - 索引祖先数据，支持全文搜索
   - 索引族谱数据，支持全文搜索
   - 提供多字段搜索
3. **搜索功能**
   - 搜索祖先数据
   - 搜索族谱数据
4. **统计功能**
   - 获取祖先数据统计
   - 姓氏分布、籍贯分布、年代分布

---

## 5. 数据流说明

### 5.1 寻根匹配流程
```
用户提交祖先数据
    ↓
系统标准化处理
    ↓
匹配引擎计算相似度
    ↓
返回匹配结果列表
    ↓
用户选择匹配宗亲
    ↓
提交认祖申请
    ↓
宗族管理员审核
    ↓
审核通过
    ↓
生成表字
    ↓
用户获得宗族身份
```

### 5.2 族谱管理流程
```
创建族谱
    ↓
设置基本信息
    ↓
添加族员信息
    ↓
设置访问权限
    ↓
上传家族文献
    ↓
用户查阅族谱
    ↓
实时更新族谱
```

### 5.3 宗亲互动流程
```
进入聊天室
    ↓
发送实时消息
    ↓
查看活动列表
    ↓
报名参加活动
    ↓
分享祖训家风
    ↓
点赞互动内容
```

---

## 6. 安全与权限

### 6.1 认证安全
- **JWT令牌**：使用JWT进行身份验证
- **密码加密**：使用bcrypt进行密码哈希
- **令牌刷新**：支持访问令牌和刷新令牌
- **令牌过期**：设置合理的令牌过期时间

### 6.2 数据权限
- **族谱权限**：查看、编辑、下载三级权限
- **数据所有权**：仅创建者可删除和完全编辑
- **审核权限**：仅宗族管理员可审核认祖申请

### 6.3 API安全
- **CORS配置**：配置合理的跨域访问策略
- **请求验证**：使用Pydantic进行输入验证
- **速率限制**：防止API滥用
- **错误处理**：统一的错误处理和响应格式

---

## 7. 部署与运行

### 7.1 前端部署
- **Web部署**：使用Quasar构建静态文件，部署到CDN或静态服务器
- **访问地址**：http://localhost:8080（开发环境）

### 7.2 后端部署
- **服务启动**：使用Uvicorn ASGI服务器
- **访问地址**：http://localhost:8000（开发环境）
- **API文档**：http://localhost:8000/docs（Swagger UI）

### 7.3 数据库
- **PostgreSQL**：关系型数据存储
- **MongoDB**：文档数据存储
- **Redis**：缓存和实时数据
- **Elasticsearch**：搜索和索引

---

## 8. 总结

本业务功能说明文档详细描述了"宗亲寻根·薪火相传APP"的所有核心功能，包括：

1. **前端五大服务模块**：认证、祖先数据、匹配、族谱、聊天
2. **后端八大控制器**：认证、祖先数据、匹配、申请、族谱、聊天、活动、文化分享
3. **大数据处理模块**：匹配引擎、数据分析器、推荐引擎、大数据存储

所有功能均按照CODE_WIKI.md文档的要求实现，提供了完整的寻根匹配、族谱管理、宗亲互动功能，以及大数据分析和推荐系统。系统采用前后端分离架构，使用现代技术栈，确保了系统的可扩展性、安全性和用户体验。

---

*文档版本：1.0*  
*最后更新：2026-04-13*
